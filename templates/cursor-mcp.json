{
  "mcpServers": {
    "claude-lore": {
      "command": "bun",
      "args": ["run", "${CLAUDE_LORE_PATH}/packages/worker/mcp.ts"],
      "env": {
        "CLAUDE_LORE_PORT": "37778"
      }
    }
  }
}
